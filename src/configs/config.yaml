train:
  epochs: 100
  accelerator: 'auto'
  devices: '1'
  seed: 42
  gradient_clip_val: 0.5
  log_every_n_steps: 2
  early_stop_patience: 40
  precision: 'high'
logger:
  mlflow_uri: http://100.64.0.15:7979
  experiment_name: AirDiffTraj
  run_name: AirDiffTraj
  tags: {'model': 'diffusion', 'dataset': 'opensky', 'weather': False}
  
model:
  type: "DDIM"
  cnn: True
  attr_dim: 32
  guidance_scale: 0
  in_channels: 8
  out_ch: 8
  ch: 128
  ch_mult: [ 1, 2, 2, 4 ]
  num_res_blocks: 6
  attn_resolutions: [16, 32]
  dropout: 0.1
  lr: 0.00002
  resamp_with_conv: True
  diffusion:
    beta_schedule: 'cosine'
    beta_start: 0.0001            # Slightly increased for faster diffusion
    beta_end: 0.08               # Slightly increased for faster diffusion
    num_diffusion_timesteps: 1000
    ema: True
    ema_rate: 0.99999
  weather_config:
    weather_grid: True
    variables: 4
    levels: 4
    lon: 5
    lat: 5
    type: "ADES"
      #type: "Function"
      #type: "Grid"
      #lon: 81
      #lat: 106
